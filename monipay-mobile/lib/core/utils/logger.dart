void logInfo(String message) {
  // TODO: Plug in proper logging (e.g. Firebase, Sentry, or custom backend).
  // For now we rely on debugPrint-style logging when used.
}

void logError(String message, [Object? error, StackTrace? stackTrace]) {
  // TODO: Plug in proper error logging.
}

